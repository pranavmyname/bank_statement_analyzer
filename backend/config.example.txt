# Environment Configuration Template
# Copy this file to .env and update the values

# Server Configuration
PORT=3001
NODE_ENV=production
CLIENT_URL=https://your-frontend-url.onrender.com

# Neon Auth Configuration (JWT-based authentication)
NEON_PROJECT_ID=your_neon_project_id_here
NEON_JWKS_URL=https://api.stack-auth.com/api/v1/projects/proj_your_project_id/.well-known/jwks.json
STACK_SECRET_SERVER_KEY=ssk_your_secret_server_key_here

# Database Configuration
# For PostgreSQL (production)
DB_URL=postgresql://username:password@hostname:port/database_name

# For SQLite (development) - leave DB_URL empty or undefined
# DATABASE_PATH=./database.sqlite

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Legacy Upload Token (for file access - keeping for backward compatibility)
UPLOAD_TOKEN=your_upload_access_token

# File Upload Configuration
MAX_FILE_SIZE=52428800
UPLOAD_DIR=./uploads

# Logging
LOG_LEVEL=info

# Frontend Environment Variables (for reference)
# Create frontend/.env with these values:
# REACT_APP_STACK_PROJECT_ID=proj_your_project_id_here
# REACT_APP_STACK_PUBLISHABLE_CLIENT_KEY=pck_your_publishable_key_here
# REACT_APP_API_URL=https://your-backend-url.onrender.com/api

# Note: For Vite projects, use VITE_ prefix instead:
# VITE_STACK_PROJECT_ID=proj_your_project_id_here
# VITE_STACK_PUBLISHABLE_CLIENT_KEY=pck_your_publishable_key_here

# Email to User ID Mapping Configuration:
# The system maps specific email addresses to specific user IDs
# Edit EMAIL_USER_MAPPING in backend/middleware/auth.js to add more mappings:
# 'pranavmyname@gmail.com' -> User ID 2 (configured)
# 'other@example.com' -> User ID 3 (example)
# Default fallback -> User ID 1
